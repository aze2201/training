# commands
kubecrl get pods -A
kubectl logs -n default alpine-nc-7b8c8b45f6-6p7w7
kubectl describe pod -n keycloak keycloak-7f4556b45-lhhnh
kubectl exec -it -n database deploy/postgres -- psql -U appuser -d appdb
kubectl port-forward svc/argocd-server -n argocd --address 0.0.0.0   8080:443

kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

kubectl rollout restart deployment postgres -n database

kubectl delete rs -n nifi --all
kubectl delete pod -n nifi --all



kubectl port-forward -n database svc/postgres 5432:5432

# create secret
kubectl create secret generic postgres-secret \
  -n database \
  --from-literal=POSTGRES_USER=appuser \
  --from-literal=POSTGRES_PASSWORD=xxxx \
  --from-literal=POSTGRES_DB=appdb

or 

kubectl apply -f secret.yaml 
'
cat secret-keycloak.yaml 
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
  namespace: keycloak
type: Opaque
stringData:
  KC_DB: postgres
  KC_DB_URL: jdbc:postgresql://postgres.database.svc.cluster.local:5432/appdb
  KC_DB_USERNAME: appuser
  KC_DB_PASSWORD: asasasasasa
  KEYCLOAK_ADMIN: admin
  KEYCLOAK_ADMIN_PASSWORD: "SuperSecretPassword"
'


kubectl edit configmap nifi-config-map -n nifi
kubectl rollout restart deployment/nifi -n nifi
