apiVersion: v1
kind: Service
metadata:
  name: nifi
  namespace: nifi
  labels:
    app: nifi
spec:
  selector:
    app: nifi
    component: dataflow
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
